# Core — everything the bot needs to trade and backtest.
# Floors exclude releases with published security advisories:
# aiohttp < 3.10.11 (request smuggling / traversal CVEs), cryptography
# < 42.0.4 (CVE-2024-26130). CI's pip-audit job gates the resolved tree.
ccxt>=4.5.0
pandas>=2.0.0
numpy>=1.26.0
aiohttp>=3.10.11
cryptography>=42.0.4  # Ed25519 signing for the Robinhood Crypto adapter

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional extras (uncomment as needed):
# lightgbm>=4.0.0        # ML meta-filter training (future work; see docs)
# prometheus-client>=0.20 # metrics endpoint
# ruff>=0.4.0            # linting
# black>=24.0.0          # formatting
