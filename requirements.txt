# Core — everything the bot needs to trade and backtest.
# Floors exclude every release with an open advisory in OSV as of
# 2026-07-17 (cryptography < 48.0.1 carries two HIGH GHSAs; aiohttp and
# pytest below the floors carry moderates). CI's pip-audit job gates the
# resolved tree on every push.
ccxt>=4.5.0
pandas>=2.0.0
numpy>=1.26.0
aiohttp>=3.14.1
cryptography>=48.0.1  # Ed25519 signing for the Robinhood Crypto adapter

# Testing
pytest>=9.0.3
pytest-asyncio>=1.0.0

# Optional extras (uncomment as needed):
# lightgbm>=4.0.0        # ML meta-filter training (future work; see docs)
# prometheus-client>=0.20 # metrics endpoint
# ruff>=0.4.0            # linting
# black>=24.0.0          # formatting
